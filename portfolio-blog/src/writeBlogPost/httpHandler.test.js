import { writeBlogPost } from './httpHandler';

describe('GIVEN: A valid HTTP request containing a blog post', () => {
  describe('WHEN: this function is called', () => {
    it('THEN: publishes the post to the database.', () => {

    });
  });
});
